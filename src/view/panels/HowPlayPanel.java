package view.panels;

import utils.ITextureFactory;
import view.main.MainView;

import javax.swing.*;
import java.awt.*;

public class HowPlayPanel extends CustomPanelTexture{

    public HowPlayPanel(JPanel parent, CardLayout panels, MainView context, ITextureFactory textureFactory) {
        super(parent, panels, context, textureFactory);
        initComponents();
    }

    private void initComponents(){

    }
}
